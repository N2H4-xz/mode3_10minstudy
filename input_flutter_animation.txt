稍等一下。诶，弄不动了。哦，我知道了。

现在好了，连接没问题。现在来解释一下这个（代码）是什么意思。

在这里，一般我们这个 Animation 这一类东西是放在我们这个状态类（State Class）里边，给它当成一个属性。

这个类，它是继承自 Animation 类。这个 Animation 呢，它是一个抽象类。

大部分我们需要用到动画的地方，它其实只需要是一个 Animation 就可以。因为 Animation 有它使用的 value 这个 getter，通过得到当前的值，它就可以来获得这个动画它实际应该放的位置。

然后呢，我们在这儿创建了它。它的这个属性，这个构造函数里边的这个属性，就是用来弄它的控制时长。

也就是说，默认情况下，我们直接创建的一个 AnimationController，它的取值是从 0.0 逐渐变化到 1.0。这是一个线性变化。大家可以看到这个方块的移动呢，它是比较规则，就是随时间它是一个线性的。

然后，这个参数是什么？这个参数……大家现在可以不用管。

你只需要知道，当我们需要在某个组件内部使用状态、使用动画的时候，我们需要给这一个状态类混入（Mixin）这么一个东西，叫什么 TickerProvider 之类的。

这一个混入语法我们之前也没讲过，但是没事，大家也先不用管。

当你只有一个动画的时候，你里边只创建一个可以这样动的东西的，你可以混入那个 SingleTickerProviderStateMixin。当你里边有两个或者以上的动画时，我们需要混入的就不是这个 Single 的了。

然后呢，我在这儿给它设置为 repeat（重复）。什么意思呢？就是让它动画重复播放。当它动画执行到末尾的时候，reverse 就是把这个值从 1.0 再变回到 0.0。

然后给它添加一个监听器（Listener）。每当它这个值变化的时候，它都会给你调用你添加的监听器。

然后我们在监听器里边 setState。因为我们 setState，并且我们这个页面里边依赖的值（这个 value）它是变化的，所以它这个（UI）就会被重绘了。重绘之后，那么就可以看到一个流畅的动画效果。

大部分情况下应该是可以做到 60 帧的。只要你这个计算没有特别复杂，或者这个页面没有特别特别难，那么你在打包成安卓包之后，它是可以到 60 帧的。

稍微有一点点卡。默认情况下，它是一个抽象类，我们需要创建一个它具体的实例。

一般情况下我们只能用 AnimationController。也不是只能用，就是说你用这个类创建出来的动画对象，你就可以控制它的启动、停止以及其他东西。比如当你调用它的函数时，它这个值就会开始变化；当你调用 stop，这个值就停止变化。

当你需要改变它这个值的上限和下限的时候，就给它传它的下界、上界。传进去，它就会按照你的要求，让这个值是线性变化的。

记住，我们目前都是一个线性变化，之后我们可以给它添加一些曲线（Curves）。

控制器。然后呢，我们就要来整一些花活了。只是这样呢，那肯定是不能满足我们的要求的。

我们目前是把它每一次值变化的时候，调用 Flutter 的函数 setState 让它重新绘制。这个写法不是那么的优雅，不是那么的美。

因为当你这个页面还有其他地方也要控制它这个状态的时候，有可能让这个计算比较复杂。

我们更好的办法是使用 AnimatedWidget（或者类似的动画组件）来进行。在这儿，这个组件。

当我们使用这个组件的时候，我们需要把我们之前创建的 Animation 对象给传进去。不管它的子类类型是什么都可以。

我们在这儿其实可以给它传递我们的实际范型参数，就是你这个里边动画它控制的变化的值是什么。我们之前是用了一个 int，我们其实也可以控制它颜色的变化。

当你给它传进去之后，当你在某个地方控制你的动画进行，那么这个组件就会帮你完成这个页面的重绘、这个部分的重绘。

那么就比我们现在这个实现方式更优雅一点，不需要我们手动来 setState。
